{
  "name": "samplepythonproject",
  "version": "0.0.1",
  "description": "Used for testing workflows.",
  "scripts": {
    "deploy": "poetry run python deploy_check.py --stage ${STAGE} --region ${REGION}",
    "smoketest": "echo \" ${SMOKETEST_URL}\""
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
